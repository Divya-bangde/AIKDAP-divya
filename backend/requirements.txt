# ===========================
# Web Framework
# ===========================
fastapi==0.116.1
uvicorn[standard]==0.35.0

# ===========================
# Configuration
# ===========================
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# ===========================
# Database
# ===========================
sqlalchemy==2.0.43
psycopg[binary]==3.2.9
alembic==1.16.4

# pgvector's SQLAlchemy `Vector` column type + psycopg codec
# registration (Sprint 9C). No transitive dependencies of its own.
pgvector==0.5.0

# ===========================
# Authentication & Security
# ===========================
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# ===========================
# Redis & Background Tasks
# ===========================
redis==6.4.0
celery==5.5.3

# ===========================
# AI Framework
# ===========================
langchain==0.3.27
langgraph==0.6.5
langchain-openai==0.3.31
langchain-community==0.3.27

# LLM gateway / model routing.
#
# Pinned to 1.80.0 rather than the latest release on purpose: litellm
# >= 1.96 requires openai>=2.20 and pydantic-settings>=2.14.1, which
# conflict with langchain-openai 0.3.31 (openai<2.0.0) and would force
# an unrelated upgrade of the settings library the whole app depends
# on. 1.80.0 is the newest release compatible with the existing pins.
litellm==1.80.0

# ===========================
# Vector Database
# ===========================
chromadb==1.0.20

# ===========================
# HTTP & Utilities
# ===========================
httpx==0.28.1
requests==2.32.5
orjson==3.11.2
tenacity==9.1.2

# ===========================
# Execution launcher (Sprint 16 Phase 7B.25) -- real Docker client.
# Import stays confined to execution_launcher/launcher.py; no other
# module in this codebase is permitted to import `docker` directly
# (see that module's docstring and test_execution_docker_isolation.py).
# ===========================
docker==7.2.0

# ===========================
# Data Processing
# ===========================
pandas==2.3.2
numpy==2.3.2
openpyxl==3.1.5
python-multipart==0.0.20
# Deterministic equation/constraint math for the Experiment Plan Engine
# (Sprint 16 Phase 6) -- equation parsing, free-symbol identification,
# and mathematically-derived constraints never go through the LLM.
sympy==1.13.3

# ===========================
# Document ingestion (Sprint 12.1)
# ===========================
# PDF text extraction. Pure Python, no system dependency (poppler/etc).
pypdf==6.16.2
# DOCX/PPTX structure-aware extraction (paragraphs+headings, slides).
python-docx==1.2.0
python-pptx==1.0.2
# HTML text extraction. lxml as the parser backend -- faster and more
# tolerant of malformed markup than the stdlib html.parser.
beautifulsoup4==4.15.0
lxml==6.1.2

# ===========================
# OCR (Sprint 12.5)
# ===========================
# Decodes embedded/standalone raster images (PNG/JPEG, and the images
# pypdf extracts from a scanned PDF page) before handing them to
# Tesseract. Already present transitively (via reportlab); pinned
# directly since this sprint imports it in production code, not just
# test fixtures.
Pillow==12.3.0
# Thin wrapper around the `tesseract` CLI binary -- no ML framework in
# the Python process itself. Chosen over EasyOCR/PaddleOCR (each pulls
# in PyTorch/PaddlePaddle, multi-GB, GPU-oriented) and over PyMuPDF-based
# rasterization (AGPL-3.0/commercial dual license -- a real risk for a
# project that may become closed-source SaaS, versus Tesseract's
# Apache-2.0). Requires the `tesseract-ocr` system package (see
# Dockerfile) -- ~19MB installed, Apache-2.0, fully offline, no API key,
# no per-call cost. See extractors.py/ocr.py for the integration.
pytesseract==0.3.13

# ===========================
# Validation & Serialization
# ===========================
email-validator==2.3.0

# ===========================
# Logging
# ===========================
structlog==25.4.0

# ===========================
# Testing
# ===========================
pytest==8.4.1
pytest-asyncio==1.1.0
pytest-cov==6.2.1
# Test-only: generates real, correctly-structured PDF fixtures for
# extractor tests (Sprint 12.1) -- pypdf reads PDFs but has no text-
# authoring API, and hand-rolling a byte-correct xref table by hand is
# exactly the kind of fragile-and-wrong-in-a-subtle-way risk not worth
# taking for a test suite that needs to be trusted.
reportlab==5.0.1

# ===========================
# Code Quality
# ===========================
black==25.1.0
isort==6.0.1
ruff==0.12.9
mypy==1.17.1

# ===========================
# Development
# ===========================
watchfiles==1.1.0