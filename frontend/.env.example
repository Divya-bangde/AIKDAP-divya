# Verified against the running backend (docker-compose.yml maps the
# backend container's port 8000 to host port 8001).
VITE_API_BASE_URL=http://localhost:8001

# Optional: research-run polling interval in milliseconds.
VITE_RESEARCH_POLL_INTERVAL_MS=1500
